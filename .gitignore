node_modules/
*.log
.DS_Store
.env
.env.local
*.tsbuildinfo

# dist/ is intentionally NOT gitignored — see README.md "How it works".
# We ship the pre-built output so drop-in installs work without a build step.
