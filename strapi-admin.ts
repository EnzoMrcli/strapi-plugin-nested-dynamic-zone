// Mirror of strapi-admin.js for environments where Vite prefers .ts at
// the plugin root. Both files must remain in sync (same ESM default
// export of the admin module).
import admin from './admin/src';
export default admin;
